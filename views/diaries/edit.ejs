<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit Diary</title>
    </head>

    <body>
        <h1>Edit Diary</h1>
        <form action="/diaries/<%= diary._id %>?_method=PUT" method="POST">
            <div>
                <label for="title">Title: </label>
                <input type="text" id="title" name="diaries[title]" value="<%= diary.title %> ">
            </div>
            <div>
                <label for="location">Location: </label>
                <input type="text" id="location" name="diaries[location]" value="<%= diary.location %> ">
            </div>
            <button>Update Diary</button>
        </form>
        <a href="/diary/<%= diary._id %>">Back to Diary</a>
    </body>

</html>